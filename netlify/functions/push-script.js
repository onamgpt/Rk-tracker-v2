const https = require("https");

const SCRIPT_CODE = "// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n// ONAM AGARBATHI \u2014 Email Intelligence + Location Tracker Apps Script\n// Version: 4.0\n// Paste this ENTIRE file into Apps Script, replacing everything\n// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n\n// \u2500\u2500 MAIN ENTRY POINTS \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nfunction doGet(e) {\n  var action = (e && e.parameter && e.parameter.action) || \"ping\";\n  try {\n    if (action === \"ping\")                return jsonOut({ ok: true, msg: \"v4.1 alive\" });\n    if (action === \"scanEmails\")          return jsonOut(scanEmails());\n    if (action === \"scanLocationEmails\")  return jsonOut(scanLocationEmails());\n    if (action === \"scanDayBookEmails\")   return jsonOut(scanDayBookEmails());\n    if (action === \"getIntel\")            return jsonOut(getIntel());\n    if (action === \"getAttachment\")       return jsonOut(getAttachment(e.parameter.gmailId, e.parameter.attachIndex, e.parameter.attachmentId, e.parameter.partId));\n    return jsonOut({ ok: false, error: \"Unknown action: \" + action });\n  } catch(err) {\n    return jsonOut({ ok: false, error: err.toString() });\n  }\n}\n\nfunction doPost(e) {\n  var body = {};\n  try { body = JSON.parse(e.postData.contents || \"{}\"); } catch(err) {}\n  var action = body.action || \"\";\n  try {\n    if (action === \"saveEntry\")  return jsonOut(saveEntry(body.entry));\n    if (action === \"logIntel\")   return jsonOut(logIntel(body.intel));\n    return jsonOut({ ok: false, error: \"Unknown POST action: \" + action });\n  } catch(err) {\n    return jsonOut({ ok: false, error: err.toString() });\n  }\n}\n\nfunction jsonOut(obj) {\n  return ContentService\n    .createTextOutput(JSON.stringify(obj))\n    .setMimeType(ContentService.MimeType.JSON);\n}\n\n// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n// SCAN EMAILS \u2014 reads #Trackerv23 purchase/sales emails\n// Reads inline images (forwarded WhatsApp photos) via Gmail API\n// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\nfunction scanEmails() {\n  // Only scan UNREAD emails \u2014 keeps it fast, picks up only newly forwarded invoices\n  // Only last 24 hours \u2014 you scan daily, no need for old emails\n  var threads = GmailApp.search(\n    \"(subject:#Trackerv23 OR subject:#trackerv23) newer_than:1d\", 0, 5\n  );\n  var results = [];\n\n  for (var t = 0; t < threads.length; t++) {\n    var msgs = threads[t].getMessages();\n    for (var m = 0; m < msgs.length; m++) {\n      var msg     = msgs[m];\n      var gmailId = msg.getId();\n      var subject = msg.getSubject();\n      var from    = msg.getFrom();\n      var date    = Utilities.formatDate(msg.getDate(), \"Asia/Kolkata\", \"yyyy-MM-dd\");\n      var body    = msg.getPlainBody().slice(0, 1200);\n      var attachments = [];\n\n      // Return attachment METADATA only \u2014 base64 fetched separately via getAttachment\n      // This keeps scanEmails fast (no base64 encoding of large images)\n      var fileAtts = msg.getAttachments();\n      for (var a = 0; a < fileAtts.length; a++) {\n        var att = fileAtts[a];\n        var ct  = att.getContentType() || \"\";\n        if (ct.indexOf(\"image\") > -1 || ct.indexOf(\"pdf\") > -1) {\n          attachments.push({\n            name:         att.getName() || \"attachment\",\n            type:         ct,\n            gmailId:      gmailId,\n            attachIndex:  a,\n            base64:       \"\",  // fetched on demand via getAttachment\n            data:         \"\"\n          });\n        }\n      }\n      // Check for inline images via Gmail API \u2014 store metadata only\n      if (attachments.length === 0) {\n        try {\n          var rawMsg = Gmail.Users.Messages.get(\"me\", gmailId, { format: \"full\" });\n          var allParts = [];\n          function flattenParts2(ps) {\n            if (!ps) return;\n            for (var i = 0; i < ps.length; i++) {\n              allParts.push(ps[i]);\n              if (ps[i].parts) flattenParts2(ps[i].parts);\n            }\n          }\n          flattenParts2((rawMsg.payload || {}).parts || []);\n          for (var p = 0; p < allParts.length; p++) {\n            var part = allParts[p];\n            var mt = part.mimeType || \"\";\n            if (mt.indexOf(\"image\") === -1 && mt.indexOf(\"pdf\") === -1) continue;\n            attachments.push({\n              name:        \"invoice.\" + (mt.split(\"/\")[1] || \"jpg\"),\n              type:        mt,\n              gmailId:     gmailId,\n              attachIndex: -1,  // inline \u2014 use partId\n              partId:      part.partId || \"\",\n              attachmentId: (part.body && part.body.attachmentId) || \"\",\n              inlineData:  (part.body && part.body.data) ? part.body.data.replace(/-/g,\"+\").replace(/_/g,\"/\") : \"\",\n              base64:      \"\",\n              data:        \"\"\n            });\n            break; // first image only for metadata\n          }\n        } catch(e) {}\n      }\n\n      // Step 3: Drive links in body\n      var driveLinks = [];\n      var driveMatch = body.match(/https?:\\/\\/(drive|docs)\\.google\\.com\\/[^\\s<\"]+/g);\n      if (driveMatch) driveLinks = driveMatch;\n\n      results.push({\n        gmailId:         gmailId,\n        subject:         subject,\n        from:            from,\n        date:            date,\n        body:            body,\n        attachments:     attachments,\n        driveLinks:      driveLinks,\n        hasInlineImage:  attachments.length > 0,\n        attachmentCount: attachments.length\n      });\n    }\n  }\n\n  Logger.log(\"scanEmails: found \" + results.length + \" emails\");\n  return { ok: true, emails: results };\n}\n\n\n// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n// SCAN DAY BOOK EMAILS \u2014 reads #DayBook tagged emails from accountant\n// Returns UNREAD only, body text (no attachments needed \u2014 Day Book is text/HTML)\n// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\nfunction scanDayBookEmails() {\n  var threads = GmailApp.search(\n    \"subject:#DayBook is:unread newer_than:14d\", 0, 5\n  );\n  var results = [];\n\n  for (var t = 0; t < threads.length; t++) {\n    var msgs = threads[t].getMessages();\n    for (var m = 0; m < msgs.length; m++) {\n      var msg = msgs[m];\n      var gmailId = msg.getId();\n      var subject = msg.getSubject();\n      var from = msg.getFrom();\n      var date = Utilities.formatDate(msg.getDate(), \"Asia/Kolkata\", \"yyyy-MM-dd\");\n      var body = msg.getPlainBody().slice(0, 8000);\n\n      // Check for attachment (Excel Day Book export)\n      var attBody = \"\";\n      var fileAtts = msg.getAttachments();\n      for (var a = 0; a < fileAtts.length; a++) {\n        var att = fileAtts[a];\n        var ct = att.getContentType() || \"\";\n        if (ct.indexOf(\"sheet\") > -1 || ct.indexOf(\"excel\") > -1 || ct.indexOf(\"csv\") > -1) {\n          try {\n            // For CSV, read as text directly\n            if (ct.indexOf(\"csv\") > -1) {\n              attBody = att.getDataAsString();\n            }\n          } catch(e) {}\n        }\n      }\n\n      results.push({\n        gmailId: gmailId,\n        subject: subject,\n        from: from,\n        date: date,\n        body: attBody || body\n      });\n    }\n  }\n\n  Logger.log(\"scanDayBookEmails: found \" + results.length + \" emails\");\n  return { ok: true, emails: results };\n}\n\n// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n// SCAN LOCATION EMAILS \u2014 reads #location emails from sales team\n// Extracts Google Maps GPS link from body\n// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\nfunction scanLocationEmails() {\n  var threads = GmailApp.search(\n    \"subject:#location newer_than:30d\", 0, 50\n  );\n  var results = [];\n\n  for (var t = 0; t < threads.length; t++) {\n    var msgs = threads[t].getMessages();\n    for (var m = 0; m < msgs.length; m++) {\n      var msg     = msgs[m];\n      var gmailId = msg.getId();\n      var subject = msg.getSubject();\n      var from    = msg.getFrom();\n      var date    = Utilities.formatDate(msg.getDate(), \"Asia/Kolkata\", \"yyyy-MM-dd\");\n      var body    = msg.getPlainBody();\n      var htmlBody = msg.getBody(); // HTML version \u2014 may have maps link\n\n      // Combine all text to search for maps link\n      var allText = subject + \" \" + body + \" \" + htmlBody;\n\n      // Extract Google Maps link\n      var mapsLinks = allText.match(/https?:\\/\\/(maps\\.google\\.com|www\\.google\\.com\\/maps|goo\\.gl\\/maps|maps\\.app\\.goo\\.gl)[^\\s<\"']*/g) || [];\n\n      results.push({\n        gmailId:   gmailId,\n        subject:   subject,\n        from:      from,\n        date:      date,\n        body:      body.slice(0, 500),\n        mapsLinks: mapsLinks,\n        allText:   allText.slice(0, 1000)\n      });\n    }\n  }\n\n  Logger.log(\"scanLocationEmails: found \" + results.length + \" emails\");\n  return { ok: true, emails: results };\n}\n\n// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n// SAVE ENTRY \u2014 saves a tracker entry row to Entries sheet\n// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\nfunction saveEntry(entry) {\n  if (!entry) return { ok: false, error: \"No entry provided\" };\n\n  var ss    = SpreadsheetApp.getActiveSpreadsheet();\n  var sheet = ss.getSheetByName(\"Entries\");\n  if (!sheet) {\n    sheet = ss.insertSheet(\"Entries\");\n    sheet.appendRow([\"ID\",\"Title\",\"Date\",\"Category\",\"Person\",\"Vendor\",\n                     \"Amount\",\"PaymentStatus\",\"Notes\",\"Tags\",\"CreatedAt\"]);\n  }\n\n  // Duplicate check via gmailId in notes\n  if (entry.notes && entry.notes.indexOf(\"gmailId:\") > -1) {\n    var gmailId = (entry.notes.match(/gmailId:([a-zA-Z0-9]+)/) || [])[1];\n    if (gmailId) {\n      var data = sheet.getDataRange().getValues();\n      for (var r = 1; r < data.length; r++) {\n        if (String(data[r][8]).indexOf(gmailId) > -1) {\n          return { ok: true, skipped: true, reason: \"duplicate\" };\n        }\n      }\n    }\n  }\n\n  sheet.appendRow([\n    entry.id            || \"\",\n    entry.title         || \"\",\n    entry.date          || \"\",\n    entry.category      || \"\",\n    entry.person        || \"\",\n    entry.vendor        || \"\",\n    entry.amount        || \"\",\n    entry.paymentStatus || \"\",\n    entry.notes         || \"\",\n    (entry.tags || []).join(\", \"),\n    entry.createdAt     || new Date().toISOString()\n  ]);\n\n  return { ok: true, saved: true };\n}\n\n// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n// LOG INTEL \u2014 saves a summary line to Intelligence sheet\n// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\nfunction logIntel(intel) {\n  if (!intel) return { ok: false, error: \"No intel provided\" };\n\n  var ss    = SpreadsheetApp.getActiveSpreadsheet();\n  var sheet = ss.getSheetByName(\"Intelligence\");\n  if (!sheet) {\n    sheet = ss.insertSheet(\"Intelligence\");\n    sheet.appendRow([\"Date\",\"Source\",\"Title\",\"Vendor\",\"Customer\",\n                     \"Amount\",\"InvoiceNo\",\"Category\",\"Summary\",\"GmailId\"]);\n  }\n\n  sheet.appendRow([\n    intel.date      || \"\",\n    intel.source    || \"email\",\n    intel.title     || \"\",\n    intel.vendor    || \"\",\n    intel.customer  || \"\",\n    intel.amount    || \"\",\n    intel.invoiceNo || \"\",\n    intel.category  || \"\",\n    intel.summary   || \"\",\n    intel.gmailId   || \"\"\n  ]);\n\n  return { ok: true, logged: true };\n}\n\n// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n// GET INTEL \u2014 returns last 100 intelligence entries\n// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\nfunction getIntel() {\n  var ss    = SpreadsheetApp.getActiveSpreadsheet();\n  var sheet = ss.getSheetByName(\"Intelligence\");\n  if (!sheet) return { ok: true, intel: [] };\n\n  var data    = sheet.getDataRange().getValues();\n  var headers = data[0];\n  var rows    = data.slice(1).slice(-100).map(function(row) {\n    var obj = {};\n    headers.forEach(function(h, i) { obj[h] = row[i]; });\n    return obj;\n  });\n\n  return { ok: true, intel: rows };\n}\n\n\n// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n// GET ATTACHMENT \u2014 fetch a single attachment by gmailId + index\n// Called separately after scanEmails to keep scan fast\n// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\nfunction getAttachment(gmailId, attachIndex, attachmentId, partId) {\n  if (!gmailId) return { ok: false, error: \"No gmailId\" };\n  try {\n    var msg = GmailApp.getMessageById(gmailId);\n    if (!msg) return { ok: false, error: \"Message not found\" };\n\n    attachIndex = parseInt(attachIndex || \"-1\");\n\n    // Case 1: regular file attachment\n    if (attachIndex >= 0) {\n      var atts = msg.getAttachments();\n      if (attachIndex < atts.length) {\n        var att = atts[attachIndex];\n        return { ok: true, base64: Utilities.base64Encode(att.getBytes()), type: att.getContentType(), name: att.getName() };\n      }\n    }\n\n    // Case 2: inline image via Gmail API\n    if (attachmentId) {\n      var attObj = Gmail.Users.Messages.Attachments.get(\"me\", gmailId, attachmentId);\n      var b64 = (attObj.data || \"\").replace(/-/g, \"+\").replace(/_/g, \"/\");\n      return { ok: true, base64: b64, type: \"image/jpeg\", name: \"invoice.jpg\" };\n    }\n\n    // Case 3: inline data already in the part\n    var rawMsg = Gmail.Users.Messages.get(\"me\", gmailId, { format: \"full\" });\n    var allParts = [];\n    function flattenA(ps) { if (!ps) return; for (var i=0;i<ps.length;i++){allParts.push(ps[i]);if(ps[i].parts)flattenA(ps[i].parts);} }\n    flattenA((rawMsg.payload || {}).parts || []);\n    for (var p = 0; p < allParts.length; p++) {\n      var part = allParts[p];\n      if ((part.mimeType||\"\").indexOf(\"image\") === -1 && (part.mimeType||\"\").indexOf(\"pdf\") === -1) continue;\n      var b64 = \"\";\n      if (part.body && part.body.data) b64 = part.body.data.replace(/-/g,\"+\").replace(/_/g,\"/\");\n      else if (part.body && part.body.attachmentId) {\n        var ao = Gmail.Users.Messages.Attachments.get(\"me\", gmailId, part.body.attachmentId);\n        b64 = (ao.data||\"\").replace(/-/g,\"+\").replace(/_/g,\"/\");\n      }\n      if (b64) return { ok: true, base64: b64, type: part.mimeType, name: \"invoice.\" + (part.mimeType.split(\"/\")[1]||\"jpg\") };\n    }\n\n    return { ok: false, error: \"No attachment found\" };\n  } catch(e) {\n    return { ok: false, error: e.toString() };\n  }\n}\n\n// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n// TEST FUNCTIONS \u2014 run these manually in Apps Script to verify\n// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\nfunction testScanEmails() {\n  var result = scanEmails();\n  Logger.log(\"Total #Trackerv23 emails: \" + result.emails.length);\n  result.emails.forEach(function(e) {\n    Logger.log(\"Subject: \" + e.subject);\n    Logger.log(\"From: \" + e.from);\n    Logger.log(\"Has image: \" + e.hasInlineImage + \" | Count: \" + e.attachmentCount);\n    Logger.log(\"Drive links: \" + e.driveLinks.length);\n    Logger.log(\"---\");\n  });\n}\n\nfunction testScanLocations() {\n  var result = scanLocationEmails();\n  Logger.log(\"Total #location emails: \" + result.emails.length);\n  result.emails.forEach(function(e) {\n    Logger.log(\"From: \" + e.from + \" | Date: \" + e.date);\n    Logger.log(\"Maps links found: \" + e.mapsLinks.length);\n    if (e.mapsLinks.length > 0) Logger.log(\"Link: \" + e.mapsLinks[0]);\n    Logger.log(\"---\");\n  });\n}\n\nfunction testPing() {\n  Logger.log(\"Script is working! v4.0\");\n}\n";
const DEPLOYMENT_ID = "AKfycbzTjkBy1M6VeOE0VKbZjlMVitP58kAwKyM1dLYvS0GGpR3pMQtpcvaPmFa7vdv-fSk9";

exports.handler = async (event) => {
  const h = {
    "Access-Control-Allow-Origin": "*",
    "Content-Type": "application/json",
    "Access-Control-Allow-Headers": "Content-Type",
    "Access-Control-Allow-Methods": "POST, GET, OPTIONS"
  };
  if (event.httpMethod === "OPTIONS") return { statusCode: 200, headers: h, body: "" };

  try {
    const CLIENT_ID     = process.env.GOOGLE_OAUTH_CLIENT_ID || process.env.google_oauth_client_id || process.env.Google_oauth_client_id;
    const CLIENT_SECRET = process.env.GOOGLE_OAUTH_CLIENT_SECRET || process.env.Google_oauth_client_secret || process.env.google_oauth_client_secret;
    const REFRESH_TOKEN = process.env.GOOGLE_APPS_SCRIPT_REFRESH_TOKEN;
    const SCRIPT_ID     = process.env.APPS_SCRIPT_ID;

    const accessToken = await getToken(CLIENT_ID, CLIENT_SECRET, REFRESH_TOKEN);

    // Step 1: Push new code
    const current = await apiCall("GET", "https://script.googleapis.com/v1/projects/" + SCRIPT_ID + "/content", null, accessToken);
    let files = current.files || [];
    const idx = files.findIndex(f => f.name === "Code");
    const newFile = { name: "Code", type: "SERVER_JS", source: SCRIPT_CODE };
    if (idx >= 0) files[idx] = newFile; else files.push(newFile);
    await apiCall("PUT", "https://script.googleapis.com/v1/projects/" + SCRIPT_ID + "/content", { files }, accessToken);

    // Step 2: Create new version
    const version = await apiCall("POST", "https://script.googleapis.com/v1/projects/" + SCRIPT_ID + "/versions",
      { description: "EmailIntel v4.0 auto-deployed " + new Date().toISOString() }, accessToken);
    const versionNumber = version.versionNumber;

    // Step 3: Update existing deployment to new version
    const deployResult = await apiCall("PUT",
      "https://script.googleapis.com/v1/projects/" + SCRIPT_ID + "/deployments/" + DEPLOYMENT_ID,
      { deploymentConfig: { versionNumber, manifestFileName: "appsscript", description: "EmailIntel v4.0" } },
      accessToken
    );

    return { statusCode: 200, headers: h, body: JSON.stringify({
      ok: true,
      msg: "EmailIntel v4.0 pushed + deployed",
      versionNumber,
      deploymentId: DEPLOYMENT_ID.slice(0,20) + "...",
      deployResult
    })};

  } catch(e) {
    return { statusCode: 500, headers: h, body: JSON.stringify({ error: e.message, stack: e.stack }) };
  }
};

function getToken(clientId, clientSecret, refreshToken) {
  return new Promise((resolve, reject) => {
    const postData = "client_id=" + encodeURIComponent(clientId) + "&client_secret=" + encodeURIComponent(clientSecret) + "&refresh_token=" + encodeURIComponent(refreshToken) + "&grant_type=refresh_token";
    const req = https.request({ hostname: "oauth2.googleapis.com", path: "/token", method: "POST", headers: { "Content-Type": "application/x-www-form-urlencoded", "Content-Length": Buffer.byteLength(postData) } }, (res) => {
      let data = "";
      res.on("data", c => data += c);
      res.on("end", () => {
        try { const p = JSON.parse(data); if (p.access_token) resolve(p.access_token); else reject(new Error("Token: " + data)); } catch(e) { reject(e); }
      });
    });
    req.on("error", reject);
    req.write(postData);
    req.end();
  });
}

function apiCall(method, url, body, token) {
  return new Promise((resolve, reject) => {
    const u = new URL(url);
    const postData = body ? JSON.stringify(body) : null;
    const req = https.request({ hostname: u.hostname, path: u.pathname, method, headers: { "Authorization": "Bearer " + token, "Content-Type": "application/json", ...(postData ? { "Content-Length": Buffer.byteLength(postData) } : {}) } }, (res) => {
      let data = "";
      res.on("data", c => data += c);
      res.on("end", () => { try { resolve(JSON.parse(data)); } catch(e) { resolve({ raw: data }); } });
    });
    req.on("error", reject);
    if (postData) req.write(postData);
    req.end();
  });
}
